extends layout 

block content 
  h1= title   
  if book_list.length 
    ul  
      each book in book_list 
        li 
           book.title
          |  (#{book.author.name})
  else 
    p Bookshelf is empty!
